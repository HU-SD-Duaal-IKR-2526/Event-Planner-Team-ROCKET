spring:
  application:
    name: core-monolith-venue
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: ${SERVER_PORT:8082}

logging:
  level:
    nl.teamrocket.core.venue: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info
